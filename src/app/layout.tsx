import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Digital Agency - Professional Web Solutions",
  description: "Leading digital agency providing professional web development, design, and marketing solutions for businesses worldwide.",
  keywords: "digital agency, web development, web design, marketing, professional services",
  authors: [{ name: "Digital Agency Team" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
